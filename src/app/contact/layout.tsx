import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact",
  description: "Contact Spartina Technology for industrial data enhancement, AI education content, visual assets, and custom AI workflow projects.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
